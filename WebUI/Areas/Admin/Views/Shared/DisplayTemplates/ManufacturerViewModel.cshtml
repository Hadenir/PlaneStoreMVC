@model ManufacturerViewModel

<table class="table table-sm table-bordered table-striped">
    <tbody>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Id)</th>
            <td>@Html.DisplayFor(m => m.Id)</td>
        </tr>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Name)</th>
            <td>@Html.DisplayFor(m => m.Name)</td>
        </tr>
    </tbody>
</table>

<table class="table table-sm table-bordered table-striped align-middle">
    <thead>
        <tr>
            <th class="text-center">Produced Aircraft</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.ProducedAircraft.Any())
        {
            @foreach (var aircraft in Model.ProducedAircraft)
            {
                <tr>
                    <td class="col-fit">
                        <a class="m-1"
                           asp-controller="Aircraft"
                           asp-action="Details"
                           asp-route-id="@aircraft.Id">
                            @Html.DisplayFor(_ => aircraft.Name)
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td class="text-center">No produced aircraft</td>
            </tr>
        }
    </tbody>
</table>